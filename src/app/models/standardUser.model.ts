export interface standardUser {
  firstName: string;
  lastName: string;
  email: string;

  name: string;
  studyProgram: string;
  semester: number;
  lectureHours: number;
  exerciseHours: number;

  classType: string;
  sessionCount: number;
}
